```mermaid
    graph LR
    subgraph "用户代码空间 (Main Function)"
        UserMapPtr[map 指针]
        UserIterPtr[iter 指针]
    end

    subgraph "堆内存 (Heap / Implementation)"
        direction TB
        
        %% Map 结构体
        MapStruct[struct Map]
        MapStruct -- "__head" --> Node1
        MapStruct -- "__tail" --> Node3
        
        %% 链表节点 (MapEntry)
        Node1[MapEntry: z=1] -- "__next" --> Node2[MapEntry: y=2]
        Node2 -- "__next" --> Node3[MapEntry: b=3]
        Node3 -- "__next" --> NULL[NULL]

        %% 迭代器结构体
        IterStruct[struct MapIter]
        
        %% 指针关系
        UserMapPtr -.-> MapStruct
        UserIterPtr -.-> IterStruct
        
        %% 迭代器核心：指向当前的某个节点
        IterStruct -- "__current" --> Node2
    end

    classDef hidden fill:#f9f,stroke:#333,stroke-width:2px;
    style MapStruct fill:#e1f5fe,stroke:#01579b
    style IterStruct fill:#fff9c4,stroke:#fbc02d
    style Node2 fill:#ffccbc,stroke:#ff5722