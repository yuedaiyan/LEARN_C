{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "CodeLLDB 调试 (完美版)",
      "type": "lldb", // <--- 核心修改：使用 CodeLLDB 引擎
      "request": "launch",
      "program": "${fileDirname}/c_build/${fileBasenameNoExtension}",
      "args": [],
      "cwd": "${fileDirname}",
      "preLaunchTask": "C/C++: gcc 生成活动文件"
    },
    {
      // --- 这是新增的 Python 配置 ---
      "name": "Python: 当前文件",
      "type": "debugpy", // 现在的 Python 插件通常使用 debugpy
      "request": "launch",
      "program": "${file}",
      "console": "integratedTerminal"
    }
  ]
}
