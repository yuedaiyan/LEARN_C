#include "k_math.h"
#include <stdio.h>
int main() {
    int a = 10;
    int b = 4;
    printf("add: %d\n", add(a, b));
    printf("mul: %d\n", mul(a, b));
    printf("sub: %d\n", sub(a, b));
    return 0;
}